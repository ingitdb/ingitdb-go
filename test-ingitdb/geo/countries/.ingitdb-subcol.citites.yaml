titles:
  en: Cities
  ru: Города

record_labels:
  en: City
  ru: Города

record_file:
  name: "{$id}/{$id}.json"
  format: json
  type: "map[string]any"

columns:
  names:
    type: map[locale]string
    required: true
    valueTitle: Name
  population:
    type: int
    title: Population
  area:
    type: int
    title: Area (km²)

min_records_count: 1
max_records_count: 100000
