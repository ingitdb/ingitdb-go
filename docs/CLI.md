# ingitdb Command Line Interface

- ```ingigdb --query [PATH] --collection=<key> [--format=CSV|JSON|YAML]``` - returns data
- ```ingigdb --validate [PATH]``` - validate inGitDB, if path is empty will use current dir.
- ```ingigdb --materialize [PATH] --views=<view1,view2>``` - materialize views
